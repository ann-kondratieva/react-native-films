export const styles = (theme) => ({
    container: {
        height: 60
    },
    selectField: {
        width: '100%',
        marginBottom: 4
    },
    textField: {
        marginLeft: theme.spacing.unit,
        marginRight: theme.spacing.unit,
        width: '100%'
    }
});